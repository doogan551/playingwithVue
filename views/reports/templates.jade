//
   Created by Soven on 9/27/2014.
script#edit-template(type="text/x-kendo-template")
    # if (data.valueType === "Float" || data.name=== "Value" ) { #
    #=data.precision#
    # } #
script#conditionTemplate(type="text/x-kendo-template")
    # if (!data.firstRow) { #
    #=data.condition#
    # } #
script#valueViewTemplate(type="text/x-kendo-template")
    # if (data.value != "" && (data.valueType == "HourMinSec"  || data.expression == "HourMinSec")) { #
    #= kendo.toString(new Date(data.value), "H 'hr' m 'min' s 'sec'") #
    #}else if (data.value != "" && (data.valueType == "HourMin"  || data.expression == "HourMin")) { #
    #= kendo.toString(new Date(data.value), "H 'hr' m 'min'") #
    #}else if (data.valueType == "MinSec" || data.expression == "MinSec"){ #
    #= kendo.toString(new Date(data.value), "m 'min' s 'sec'") #
    #}else if (data.valueType == "Float"){ #
    #= kendo.toString(data.value, "{0:n2}") #
    # }else if (data.value != "" && (data.valueType == "DateTime" || data.valueType == "Timet")){ #
    #= kendo.toString(new Date(data.value), "g") #
    # }else{ #
    #=data.value #
    # } #
